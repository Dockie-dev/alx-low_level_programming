#include "main.h"
/**
 * _isupper -check the code if uppercase character
 * @c: variable text
 * Return: Always if 0.
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);

}
else
{
	return (0);
